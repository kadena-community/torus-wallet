/* import React, { useContext } from 'react';
import { BrowserRouter as Router, Route, Redirect, useHistory, Switch } from 'react-router-dom'; */

/* import {
  ROUTE_DASHBOARD,
  ROUTE_LOGIN
} from './routes'; */




const AppRouter = () => {

  return (
    {/* <Router>
    
          <Route exact path={ROUTE_LOGIN} exact component={LoginPage} >
            <Redirect to={ROUTE_DASHBOARD} exact component={DashboardContainer}/>
          </Route>
          
    </Router> */}
  );
};

export default AppRouter;
